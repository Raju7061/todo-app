apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring-deploy
  namespace: flux-system
spec:
  interval: 2m0s
  path: "./k8s/monitoring"
  prune: true
  sourceRef:
    kind: GitRepository
    name: todo-app-source
  # targetNamespace nahi — monitoring files mein khud namespace: monitoring likha hai